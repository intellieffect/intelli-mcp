# NPM configuration for MCP Config Manager v2

# Use legacy peer deps to resolve conflicts
legacy-peer-deps=true

# Suppress audit warnings during install
audit-level=high

# Don't save exact versions
save-exact=false

# Use the npm registry
registry=https://registry.npmjs.org/